export const DELEGATE_DESCRIPTION = `把 1-2 个子任务委派给独立上下文运行的子代理并行执行；本 Tool 同步等待全部完成，返回每个子任务的结构化摘要。

两种任务类别：
- exploring：只读探索——检索与阅读代码、收集信息、整理发现，不能修改任何文件
- writing：可写实现——编写代码、运行命令、完成改动并自行验证

何时使用：
- 子任务需要大量检索/阅读步骤，全部带入当前上下文会造成污染（exploring）
- 相对独立、能自包含描述的编码改动（writing）
- 两个互不依赖的子任务可以并行推进

何时不使用：
- 几个 Tool Call 就能亲自完成的事（单次 grep/read、小改动）
- 只是想"换个角度再想一遍同一个问题"——子代理的价值在新信息与专注执行，不在重复思考

规则：
- 每批最多 2 个子任务，且最多 1 个 writing
- 子代理看不到当前对话：brief 必须自包含（目标、范围、期望返回）；context 传已确认的事实与约束；references 传文件路径
- exploring 的重要结论应带 file:line 证据；writing 完成前必须自证（运行/测试）
- 返回的 XML 是每个子任务的摘要，不是其完整执行过程；需要细节时按摘要中的文件路径自行查看`;
