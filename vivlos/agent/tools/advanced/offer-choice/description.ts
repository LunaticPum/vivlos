export const description = `向用户展示选项并等待选择。适用于需要用户决策或澄清意图的场景。

## 何时使用
- 需要用户在多个方案/策略中选择
- 需要澄清用户意图（"你指的是哪个？"）
- 用户意图不明确，给出可能选项比直接反问更高效

## 何时不用
- 可以直接回答的问题
- 只有一个合理选项（直接做，不要问）
- 选项过多（>10 个）时考虑换一种交互方式

## 参数
- prompt: 问题描述/上下文（必填）
- choices: 选项列表，2-10 个（必填）
- multiple: 是否允许多选（默认 false）

## 规则
- 选项要具体且互斥
- prompt 要说清为什么需要用户选择
- 不要用 offer_choice 替代简单的 yes/no（直接做或直接问）`;
